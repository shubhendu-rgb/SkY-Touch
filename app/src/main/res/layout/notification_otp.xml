<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:background="@android:color/transparent">

    <!-- OTP Icon on the Left -->
    <ImageView
        android:id="@+id/notification_otp_icon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_centerVertical="true"
        android:src="@drawable/ic_otp_notification"
        android:contentDescription="OTP Icon"
        android:layout_alignParentStart="true"
        android:tint="@color/notification_icon_tint" />

    <!-- Text Container in the Center -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:orientation="vertical"
        android:layout_toEndOf="@id/notification_otp_icon"
        android:layout_toStartOf="@id/notification_copy_btn"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Code • Expiring in "
                style="@style/TextAppearance.Compat.Notification.Info"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium" />
            
            <Chronometer
                android:id="@+id/notification_timer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/TextAppearance.Compat.Notification.Info"
                android:textColor="?android:attr/colorError"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium" />
        </LinearLayout>

        <TextView
            android:id="@+id/notification_code_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="123456"
            style="@style/TextAppearance.Compat.Notification.Title"
            android:textSize="22sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-black" />
    </LinearLayout>

    <!-- Large Copy Button on the Right -->
    <ImageButton
        android:id="@+id/notification_copy_btn"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_copy_action"
        android:contentDescription="Copy OTP Code"
        android:scaleType="centerInside"
        android:padding="12dp"
        android:tint="@color/notification_icon_tint" />

</RelativeLayout>
